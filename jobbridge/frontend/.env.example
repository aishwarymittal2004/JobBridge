# Not required for local dev (Vite proxies /api to the backend, see vite.config.js).
# Set VITE_API_URL if you need to point the built frontend at a different backend origin.
VITE_API_URL=http://localhost:8000
